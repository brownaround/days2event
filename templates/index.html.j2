{% extends "base.html" %}
{% block content %}
  {% for event in events %}
    <div>
      <h2>{{ event.name }}</h2>
      <p>{{ event.dates }} · {{ event.location }}</p>
      <a href="{{ event.link }}" target="_blank">Official Site</a>
    </div>
  {% endfor %}
{% endblock %}